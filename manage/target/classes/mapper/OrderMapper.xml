<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.OrderMapper">
    <resultMap id="OrderResultMap" type="com.mall.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="pay_type" property="payType"/>
        <result column="pay_time" property="payTime"/>
        <result column="order_status" property="orderStatus"/>
        <result column="consignee" property="consignee"/>
        <result column="phone" property="phone"/>
        <result column="address" property="address"/>
        <result column="remark" property="remark"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <collection property="orderItems" ofType="com.mall.entity.OrderItem" column="id" select="selectOrderItemsByOrderId"/>
    </resultMap>

    <resultMap id="OrderItemResultMap" type="com.mall.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectOrderItemsByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM order_item WHERE order_id = #{id}
    </select>

    <select id="selectById" resultMap="OrderResultMap">
        SELECT o.*, u.username
        FROM "order" o
        LEFT JOIN "user" u ON o.user_id = u.id
        WHERE o.id = #{id}
    </select>

    <select id="selectByOrderNo" resultMap="OrderResultMap">
        SELECT o.*, u.username
        FROM "order" o
        LEFT JOIN "user" u ON o.user_id = u.id
        WHERE o.order_no = #{orderNo}
    </select>

    <select id="selectList" resultMap="OrderResultMap">
        SELECT o.*, u.username
        FROM "order" o
        LEFT JOIN "user" u ON o.user_id = u.id
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND o.order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="orderStatus != null">
                AND o.order_status = #{orderStatus}
            </if>
        </where>
        ORDER BY o.create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO "order" (order_no, user_id, total_amount, pay_amount, pay_type, pay_time, 
                            order_status, consignee, phone, address, remark)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{payAmount}, #{payType}, #{payTime},
                #{orderStatus}, #{consignee}, #{phone}, #{address}, #{remark})
    </insert>

    <update id="update">
        UPDATE "order"
        <set>
            <if test="orderStatus != null">order_status = #{orderStatus},</if>
            <if test="consignee != null">consignee = #{consignee},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="address != null">address = #{address},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateOrderStatus">
        UPDATE "order" SET order_status = #{orderStatus}, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM "order" WHERE id = #{id}
    </delete>
</mapper>